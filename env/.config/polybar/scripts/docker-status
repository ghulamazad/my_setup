#!/bin/bash
if command -v docker >/dev/null 2>&1; then
    running=$(docker ps -q 2>/dev/null | wc -l)
    if [ "$running" -gt 0 ]; then
        echo " ${running}"
    else
        echo ""
    fi
else
    echo ""
fi
